local project = {
    alias = "rrg",
    namespace = "rrg",
    types = { "tool" },
    additional_types = { "test" },
    dependencies = {
        "public rythe/rythe-standard-library",
        "private third_party/clang-c:header-only"
    },
    defines = { "RYTHE_INTERNAL", "RSL_NO_LOGGING_DEBUGBREAK" },
    additional_link_targets = { "libclang" },
    pch_file_name = "./pch"
}

function project.init(proj, ctx)
    proj.debug_args = {
        "--v",
        "--c",
        "--intermediates=\"$obj_path$\"",
        "--arg=\"-isystem..\\..\\..\\libraries\\rythe\\rythe-standard-library\\src\"",
        "--arg=\"-std=c++23\"",
        os.realpath(proj.location .. "/src/test/test.hpp")
    }
    
    return proj
end

return project
