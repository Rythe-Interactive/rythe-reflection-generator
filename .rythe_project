local project = {
    alias = "rrg",
    namespace = "rrg",
    types = { "tool" },
    additional_types = { "test" },
    dependencies = {
        "public rythe/rythe-standard-library",
        "private third_party/clang-c:header-only"
    },
    defines = { "RYTHE_INTERNAL" },
    additional_link_targets = { "libclang" },
    pch_file_name = "./pch"
}

function project.init(proj, ctx)
    proj.debug_args = {
        "--v",
        "--pch=\"$pch_bin$\"",
        "--i=\"$obj_path$\"",
        os.realpath(proj.location .. "/src/test/test.hpp")
    }
    
    return proj
end

return project
